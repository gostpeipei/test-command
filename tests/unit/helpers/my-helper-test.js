
import { myHelper } from 'library-app/helpers/my-helper';
import { module, test } from 'qunit';

module('Unit | Helper | my helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = myHelper([42]);
  assert.ok(result);
});

