import Ember from 'ember';

export default Ember.Route.extend({
	// setupController() {
	// 	console.log('404')
	// 	this._super(...arguments);
	// 	this.controllerFor('application').set('isShowNavbar', false)
	// }
	isShowNavbar: false
});
